params {
    max_cpus   = 10
    max_memory = 500.GB
    max_time   = 24.h
}

executor {
    name   = 'local'
    cpus   = 10
    memory = '500 GB'
}

process {
    executor = 'local'
}

env {
    TMPDIR              = '$TMPDIR'
    APPTAINERENV_TMPDIR = '$TMPDIR'
}
